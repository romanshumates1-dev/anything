# AUDIT_2026-07-21.md — DealFlow AI Prompt 1 Phase 0 (full repo audit)

**Method:** (1) idempotent migration run against dev DB (found+fixed #23–26); (2) live unauthenticated HTTP sweep of all 103 API routes + 43 pages against `next dev` on :4000 (observed status codes below); (3) 15-agent static analysis workflow (8 route groups + 7 capability audits, 539 tool calls) with claims spot-verified against the live information_schema; (4) two agent claims REFUTED by live evidence and corrected inline — static rows not yet live-reproduced are marked SUSPECT, not BROKEN.

**Audited tree:** commit c8c2744 (parallel-session SaaS build landed) + Phase 0 migration fixes. Predecessor doc `AUDIT_2026-07-20.md` is the parallel session PRE-implementation snapshot — superseded by this file.

## 1. Repo map

| Piece | Reality |
|---|---|
| Workspaces | `apps/web` (Next 16 App Router, port 4000), `apps/desktop` (Electron), `apps/mobile` (Expo — untouched this sprint) |
| DB | Neon serverless Postgres (`@neondatabase/serverless` 0.10.4, raw tagged SQL — **no composable fragments**; that API is neon v1+). **59 live tables**; 34 migrations, `scripts/migrate.mjs` canonical (re-applies all files each run → every file MUST be idempotent) |
| Queue/workers | **Postgres `jobs` table** (dedupe_key partial unique, locked_until) drained by `apps/web/scripts/worker.mjs` (prod) / jobs-dev loop → `POST /api/jobs/process`. **There is NO BullMQ and NO Redis anywhere** — Prompt 1 preamble is wrong on this, as it is about pnpm (repo uses yarn 4) |
| Auth | better-auth (session cookie; `user.role` ∈ ADMIN/MEMBER via access-control.ts; `requireAdmin` re-reads role from DB per call; middleware MIN_ACCESS_ROLE gate) |
| Payments | `services/stripeProvider.ts` abstraction — mock provider default (`STRIPE_PROVIDER`), live driver is a **stub** (verifyWebhook compares literal strings) |
| Env | ~70 vars consumed; **zero boot-time validation** (preflight.mjs is opt-in, 8 vars); Stripe/e-sign vars missing from .env.example; 4 stale TWILIO_TEST_*/TOLL_FREE_* entries. **zod is not a dependency of apps/web at all** |

## 2. Route inventory — all 101 API routes

Classification: **67 WIRED / 31 SUSPECT / 3 STUBBED**. "Sweep" = observed HTTP status from the live unauthenticated GET sweep (401/405 = properly gated; only /api/system/health, /api/legal, /api/openapi, /api/simulator, /api/system/cron GETs are public by design or accident).

| Route | Methods | Auth | Valid. | Sweep | Status | Notes |
|---|---|---|---|---|---|---|
| `/api/__create/check-social-secrets` | GET | none | manual | 404 | **WIRED** | Dev-only platform shim; reports missing google/apple OAuth env keys by name only, no values leaked. |
| `/api/admin/audit-log` | GET | admin | n/a | 401 | **WIRED** | Table/columns exist (034). Bug: inner const shadows outer `total`, so pagination.total is always undefined. |
| `/api/admin/bans` | GET,POST,DELETE | admin | manual | 401 | **SUSPECT** | `bans` table exists in NO migration/schema — all methods 500. GET also selects o."createdAt" (column is created_at). DELETE 404s even on success (UPDATE without RETURNING). |
| `/api/admin/exports` | GET | admin | manual | 401 | **SUSPECT** | Default organizations export broken: o."createdAt" wrong (created_at) and u.organization_id doesn't exist on user. payments/usage exports look valid. |
| `/api/admin/exports/finance` | GET | admin | n/a | 401 | **SUSPECT** | `transactions` table exists nowhere in schema/migrations — always 500. CSV rows also unescaped (naive string join). |
| `/api/admin/organizations` | GET,PATCH | admin | manual | 401 | **SUSPECT** | GET 500s: o."createdAt" wrong (created_at) and u.organization_id doesn't exist on user. PATCH name-update works; `status` param silently ignored. |
| `/api/admin/reviews` | GET,POST | admin | manual | 401 | **WIRED** | Tables/columns match 033. POST validates action whitelist; doesn't require review be pending (can flip approved/rejected). |
| `/api/admin/subscriptions` | GET,PATCH | admin | manual | 401 | **SUSPECT** | GET selects os.stripe_subscription_id; real column is payment_processor_subscription_id (026) — 500. PATCH status unvalidated vs CHECK enum. Documented POST /cancel not implemented. |
| `/api/admin/users` | GET | admin | n/a | 401 | **WIRED** | Columns match better-auth camelCase schema (schema.sql, 003). Hard LIMIT 500, no pagination. |
| `/api/admin/users/[id]` | PATCH | admin | manual | 405 | **WIRED** | Role whitelist via KNOWN_ROLES, last-admin lockout guard, session revocation on demotion. Solid. |
| `/api/ai-providers` | GET,POST | user | manual | 401 | **SUSPECT** | API keys 'encrypted' as base64. POST claims add/update but always INSERTs — repeat saves create duplicate provider rows. Only providerType validated. |
| `/api/analytics` | GET | user session, org-scoped | n/a | 401 | **WIRED** | Columns verified (test_mode m001, assignment_fee_cents m004); revenue mixes real fees with env-estimated fee, labelled. Org fallback 'default' vs seed 'org_default' may scope to zero rows. |
| `/api/analytics/geo` | GET | admin requireAdmin | n/a | 401 | **WIRED** | Area-code-derived regions; capped 5000-row scan. |
| `/api/approvals` | GET | user — better-auth sessi | n/a | 401 | **WIRED** | Org falls back to 'default' when session.user.organizationId is unset — cross-tenant bleed risk for legacy users. |
| `/api/approvals/[id]` | POST | user — better-auth sessi | manual | 405 | **WIRED** | Money route; idempotency guards solid. min silently defaults to 85% of max when ai_min absent. Same org='default' fallback. |
| `/api/approvals/count` | GET | user — better-auth sessi | n/a | 401 | **WIRED** | Counts PENDING rows in both queues; matches /approvals page shape. |
| `/api/auth/[...all]` | GET,POST | mixed — better-auth catc | n/a | 401 | **WIRED** | toNextJsHandler(auth) on pg pool; domain-allowlist hooks on sign-up/sign-in in lib/auth.ts. |
| `/api/auth/expo-web-success` | GET | user — session cookie | n/a | — | **WIRED** | postMessage targetOrigin '*' leaks session JWT to any embedding parent frame. |
| `/api/auth/token` | GET | user — session cookie | n/a | 401 | **WIRED** | Returns raw better-auth session token as 'jwt' for mobile SecureStore exchange. |
| `/api/campaigns` | GET,POST | user session, no role ch | manual | 401 | **WIRED** | No tenant scoping: any signed-in user sees/creates all campaigns. daily_cap/throttle_per_minute not settable on create. |
| `/api/campaigns/[id]/launch` | POST | user session, no role ch | manual | 405 | **WIRED** | Real job enqueue with dedupe+throttle; but writes self-attested 'pass' rows to execution_runs. Any member can launch. |
| `/api/campaigns/[id]/leads` | GET,POST | user session, no role ch | manual | 401 | **WIRED** | Nonexistent leadId triggers FK violation -> 500 instead of 400. No tenant scoping. |
| `/api/contact` | POST | none | manual | 405 | **WIRED** | Comment says 'Zod validation' but checks are manual. Operator notification email is TODO. In-memory rate limit resets per instance. |
| `/api/contact-lists` | GET,POST | user — session; org id = | manual | 401 | **WIRED** | Stores session.user.id as organization_id — user-scoped, not org-scoped; inconsistent with organizations model, lists invisible to org-mates. consent_mode/source_type not whitelisted. |
| `/api/contracts` | GET | user — better-auth sessi | n/a | 401 | **WIRED** | Batched esign_events join; columns (esign_status, inspection_days, assigned_at) all exist via migrations 016/018. |
| `/api/conversations` | GET | user | n/a | 401 | **WIRED** | No tenant scoping: ai_conversations lacks organization_id, so any authed user sees all orgs' conversations. |
| `/api/conversations/[leadId]` | GET | user | manual | 401 | **WIRED** | Integer param check present; same missing tenant scoping as list route. |
| `/api/conversations/message` | POST | user | manual | 405 | **WIRED** | Real AI call + consent + risk gate; leadId type unchecked; no org scoping — any authed user can message any lead. |
| `/api/conversations/thread` | GET | user | none | 401 | **SUSPECT** | Dead legacy: reads params.leadId on static route (undefined, Promise not awaited); campaign_contacts.lead_id column doesn't exist → SQL error 500. |
| `/api/conversations/thread/[leadId]` | GET | user | manual | 401 | **WIRED** | Replacement for dead static thread route; all messages share one last_message_at timestamp; no org scoping. |
| `/api/crm/contacts` | GET | user — session; org from | n/a | 401 | **WIRED** | Org fallback hardcoded 'default' (session.user.organizationId likely absent); inconsistent with 'org_default' used by leads backfill and getOrganization(). |
| `/api/crm/contacts/[id]` | GET,PATCH | user — session; org-scop | manual | 401 | **WIRED** | Conversation joined by phone; leads join has no org filter — can surface another tenant's conversation for a shared phone. negotiation_id=contact id is the intended model. |
| `/api/dashboard/stats` | GET | user session | n/a | 401 | **WIRED** | Global counts — no organization_id filter; any logged-in user sees whole-instance totals. |
| `/api/esign/mock-sign` | GET | none | manual | 400 | **SUSPECT** | Unauthenticated contract mutation live in prod — only guard is 'mock_env_' prefix. No org scoping; bypasses status machine; query params reflected into HTML. |
| `/api/esign/webhook` | POST | signature — provider fro | manual | 405 | **SUSPECT** | Default mock provider accepts any signature; documenso/docusign checks are hardcoded strings → forgeable. collect_now payment branch dead: fee_collection/fee_config never SELECTed. |
| `/api/funnel` | GET | user session, org-scoped | n/a | 401 | **SUSPECT** | Joins st.contract_id — column does not exist (021 has lead_id/campaign_id only); also GROUP BY to_stage ORDER BY st.occurred_at is invalid SQL. Route 500s. |
| `/api/imports` | GET | user — session only | n/a | 401 | **WIRED** | No tenant scoping and imports table has no organization_id column — every authed user sees all import history. |
| `/api/jobs/process` | GET,POST | token JOB_RUNNER_SECRET  | n/a | 401 | **WIRED** | Real queue drain (drainJobs(25) in utils/jobs.ts). |
| `/api/lead-finder/create-campaign` | POST | admin — requireAdmin, ro | manual | 405 | **SUSPECT** | Same defect as /api/leads/bulk: leads insert omits NOT NULL organization_id (migration 030) — handoff insert fails; sourced_leads already claimed 'handed_off' before failing insert. |
| `/api/lead-finder/sourced-leads` | GET | admin — requireAdmin | n/a | 401 | **WIRED** | Clean; capped limit 500; filters parameterized. |
| `/api/lead-finder/sources` | GET,POST | admin — requireAdmin | manual | 401 | **WIRED** | Enum whitelists + compliance guard (no PERMITTED without robots check). Unique-violation detected by regex on error message — brittle. |
| `/api/lead-finder/sources/[id]` | PATCH,DELETE | admin — requireAdmin | manual | 405 | **WIRED** | Read-then-update not transactional (lost-update race on concurrent PATCH); otherwise sound. |
| `/api/lead-finder/upload` | POST | admin — requireAdmin | manual | 405 | **WIRED** | Per-row INSERT loop (slow for big files); parse failures returned/counted but not persisted anywhere, unlike /api/leads/bulk. |
| `/api/leads` | GET,POST | user — better-auth sessi | manual | 401 | **WIRED** | Solid: org-scoped reads/writes, type whitelist, parameterized dynamic filter. |
| `/api/leads/[id]/ai` | POST | user — session only, NO  | manual | 405 | **WIRED** | IDOR: no organization_id filter — any authed user can toggle ai_paused on any tenant's lead. |
| `/api/leads/bulk` | POST | user — session only, no  | manual | 405 | **SUSPECT** | INSERT INTO leads omits organization_id; migration 030 made it NOT NULL with no default — insert fails at runtime. Dedupe also cross-tenant. |
| `/api/leads/export` | GET | user — session; org from | n/a | 401 | **WIRED** | Misnamed: exports campaign_contacts, not leads. Org fallback hardcoded 'default' via (session.user as any).organizationId — likely always 'default'. Not streamed despite comment; 10k LIMIT. |
| `/api/legal` | GET,POST | none | manual | 200 | **WIRED** | POST unauthenticated and trusts client-supplied userId — acceptance records forgeable for any user. Docblock says /api/legal/accept but handler lives here. |
| `/api/metrics` | GET | none | n/a | 500 | **SUSPECT** | Queries ai_requests, which has no migration anywhere in db/ — sql.transaction fails, endpoint always 500s. Also exposes ops data unauthenticated if fixed. |
| `/api/negotiation/analyze` | POST | admin — requireAdmin + n | manual | 405 | **WIRED** | Only checks inputs presence; Number() coercion lets NaN fields reach the AI prompt. Result not persisted, only audit-logged. |
| `/api/negotiation/preview` | POST | admin — requireAdmin + n | manual | 405 | **WIRED** | Pure valuationEngine compute against a stored profile; defaults every missing input to 0/'moderate'. |
| `/api/negotiation/profiles` | GET,POST | admin — requireAdmin + n | manual | 401 | **WIRED** | validateProfileInput enforces ranges; id derived from name (dup names collide on PK insert, unhandled 500). |
| `/api/negotiation/profiles/[id]` | GET,PATCH | admin — requireAdmin + n | manual | 401 | **WIRED** | PATCH validates via dummy-name trick; read-modify-write update, no concurrency guard. Versions table exists but PATCH relies on DB trigger for snapshots. |
| `/api/negotiation/sessions` | GET,POST | admin — requireAdmin; GE | manual | 401 | **WIRED** | AGENT CLAIM REFUTED: Phase A live-proven (a4-verify 9/9); ranges live in negotiation_price_ranges, not owner_range_requests columns. |
| `/api/negotiation/sessions/[id]/pause` | POST | admin — requireAdmin, de | n/a | 405 | **WIRED** | Not org-scoped: any admin can pause any org's session; returns paused:true even for nonexistent session ids. |
| `/api/openapi` | GET | none | n/a | 200 | **WIRED** | Static hand-authored OpenAPI 3.0.3 doc; only contacts schemas are detailed, other paths are bare stubs; can drift from actual v1 routes. |
| `/api/organizations` | GET,POST | user | manual | 401 | **WIRED** | 3 inserts not in a transaction (partial org on failure). Trial insert FK-requires 'plan_starter' from db/seeds/subscription_plans.sql being run. |
| `/api/outreach/campaigns` | GET,POST | user session, no role ch | manual | 401 | **SUSPECT** | OPENING template inserted without campaign_id, so start/dispatchOpenings never finds it. session.user.organizationId never set -> all orgs collapse to 'default'. |
| `/api/outreach/campaigns/[id]/cancel` | POST | user session, no role ch | n/a | 405 | **SUSPECT** | params not awaited (Next 16 Promise params): params.id undefined -> always 404. Org falls back to 'default'. |
| `/api/outreach/campaigns/[id]/contacts` | POST | user session, no role ch | manual | 405 | **SUSPECT** | Broken twice: params.id undefined (Next 16), and batch INSERT misaligns placeholders (idx*4 base, 6 args vs 5 slots) -> bind error every call. |
| `/api/outreach/campaigns/[id]/pause` | POST | user session, no role ch | n/a | 405 | **SUSPECT** | params not awaited (Next 16): params.id undefined -> always 404. Otherwise a simple status flip. |
| `/api/outreach/campaigns/[id]/resume` | POST | user session, no role ch | n/a | 405 | **SUSPECT** | params not awaited (Next 16): params.id undefined -> always 404. End-date/COMPLETED logic otherwise sound. |
| `/api/outreach/campaigns/[id]/start` | POST | user session, no role ch | n/a | 405 | **WIRED** | Correctly awaits params; real DNC audit + status transition. But dispatchOpenings is flag-gated and finds no OPENING for campaigns created via POST (campaign_id NULL). |
| `/api/outreach/campaigns/[id]/stats` | GET | user session, no role ch | n/a | 401 | **SUSPECT** | params not awaited (Next 16): params.id undefined -> always 404. dailyHistory LIMIT 60 ASC returns oldest days, not latest. |
| `/api/outreach/inbound` | POST | user session | manual | 405 | **SUSPECT** | Session-auth JSON endpoint — carriers can't call it (simulator). Intent handling is placeholder regex with 'In production' TODO; advisory contact lock is a no-op over neon HTTP. |
| `/api/outreach/scheduler` | POST | user session, no role ch | n/a | 405 | **STUBBED** | Marks contacts SENT and increments daily counts but never sends or enqueues any message — pure status simulation. Org falls back to 'default'. |
| `/api/payments` | GET,POST | user | manual | 401 | **SUSPECT** | GET?contractId skips org check (cross-tenant read). Org hardcoded 'default' (session lacks organizationId). Live Stripe driver is commented-out stub. |
| `/api/payments/mock-checkout` | GET | none | manual | 400 | **STUBBED** | Dev mock checkout page; no NODE_ENV gate. Template bug: '#{contractId.slice(0,8)}' missing $. contractId/amount injected into HTML unescaped (reflected XSS). |
| `/api/payments/mock-checkout/complete` | POST | none | manual | 405 | **SUSPECT** | Unauthenticated, no env gate: anyone with pi_mock id flips ledger to paid. Prod-reachable since STRIPE_PROVIDER defaults to mock. contractId echoed unescaped. |
| `/api/payments/refund` | POST | user | manual | 405 | **SUSPECT** | Ledger-only 'refund' — never calls Stripe, no money moves. Same 'default' org fallback; contracts under real org ids would 404. |
| `/api/payments/webhook` | POST | signature | manual | 405 | **SUSPECT** | Mock verifyWebhook always true (default mode) — unauthenticated callers mark payments paid. Live stub checks signature==='stripe-valid'. 'payment_intent.refunded' is not a real Stripe event. |
| `/api/reviews` | GET,POST | mixed: GET public, POST  | manual | 500 | **SUSPECT** | GET SQL broken: demoFilter string nested as sql fragment binds as parameter (syntax error); unqualified id/created_at/status ambiguous after user join. Prod demo-filter never applies. |
| `/api/session` | GET | user — session cookie | n/a | 401 | **WIRED** | Echoes full session.user/session objects; no field filtering. |
| `/api/settings/ai-provider` | GET,PUT | admin — requireAdmin, DB | manual | 401 | **WIRED** | Provider enum checked; ollamaBaseUrl accepted unvalidated as URL (SSRF surface if admin compromised). |
| `/api/settings/api-keys` | GET,POST | admin — requireAdmin + s | manual | 401 | **WIRED** | organization_id = session.user.id (user-as-org); scopes array not validated against known scope list. |
| `/api/settings/api-keys/[id]` | DELETE | admin — requireAdmin + s | n/a | 405 | **WIRED** | Soft revoke (revoked=true), org-scoped; params Promise correctly awaited (Next 16). |
| `/api/settings/beta-flags` | GET,PATCH | admin — requireAdmin | manual | 401 | **WIRED** | PATCH whitelists BETA_FLAG_KEYS booleans only; cache busted on write; changes audit-logged. |
| `/api/settings/number-pool` | GET,POST,PATCH | admin — requireAdmin | manual | 401 | **WIRED** | E.164 regex, enum defaults, cap range 0-100000 checked; no duplicate-number conflict response noted. |
| `/api/simulator` | GET,POST | none | manual | 200 | **STUBBED** | Pure Math.random outcomes, never touches pipeline; 'hybrid' Claude mode never calls Claude (claudeCallsUsed always 0); unauthenticated writes to audit/ledger. |
| `/api/sms/inbound` | POST | mixed: Twilio HMAC signa | manual | 405 | **SUSPECT** | Dedup queries audit_logs.metadata but column is payload → SQL error → 500 on every real Twilio webhook; JSON/simulator branch works. |
| `/api/subscriptions` | GET,POST | user | manual | 401 | **SUSPECT** | Plan changes have zero payment/Stripe step — upgrades free. cancel dereferences currentSub.id without null check (500 if no active sub). Proration TODO comment. |
| `/api/system/ai-status` | GET | admin requireAdmin | n/a | 401 | **WIRED** | Live provider ping (Ollama tags / 1-token Anthropic call); honest green/red. |
| `/api/system/cron` | GET,POST | token x-cron-secret, fai | manual | 200 | **WIRED** | log-cleanup task uses DELETE...RETURNING count(*) — aggregates invalid in RETURNING, task always 500s. Other 3 tasks fine. GET is unauthenticated task listing. |
| `/api/system/dashboard` | GET | admin requireAdmin | n/a | 401 | **WIRED** | Real tile aggregation; worker tile is config-echo only (no live worker probe, documented). |
| `/api/system/database` | GET | admin requireAdmin | n/a | 401 | **WIRED** | poolSize:10 is hardcoded, not measured. |
| `/api/system/event-log` | GET | admin requireAdmin | manual | 401 | **WIRED** | Phone masking + payload from audit_logs; integration filter via LIKE prefix; limit clamped to 200. |
| `/api/system/health` | GET | none | n/a | 200 | **WIRED** | Booleans-only liveness; uptime resets per serverless instance (module-scope START_TIME). |
| `/api/system/metrics` | GET | admin requireAdmin | n/a | 401 | **WIRED** | errors count via LIKE '%error%'/'%failed%' double-counts message_failed; uptimeSec is per-instance, not app uptime. |
| `/api/system/queue-status` | GET | admin requireAdmin | n/a | 401 | **WIRED** | workers:1 hardcoded. |
| `/api/system/readiness` | GET | admin requireAdmin | n/a | 401 | **WIRED** | Route/UI-flow counts are static self-asserted manifests (always 100%); DB/jobs/flows are live queries. |
| `/api/test-phones` | GET,POST | user; orgId = session.us | manual | 401 | **WIRED** | OTP hashed, rate-limited (3/hr, 5 verified cap), DNC-gated; no E.164 phone format validation; user.id used as organization_id. |
| `/api/test-phones/[id]` | DELETE | user; orgId = session.us | n/a | 405 | **WIRED** | Org-scoped fetch+delete; params correctly awaited (fixed prior Next 16 Promise-params bug). |
| `/api/test-phones/[id]/verify` | POST | user; orgId = session.us | manual | 405 | **WIRED** | SHA-256 hash compare, 10-min expiry, 3-attempt cap; code field shape not validated (coerced to String). |
| `/api/twilio-accounts` | GET,POST | user | manual | 401 | **SUSPECT** | 'Encryption' is base64 placeholder — Twilio SID/token effectively plaintext at rest. quota_used hardcoded to quota value, not computed. |
| `/api/v1/analytics` | GET | token API key + read:ana | n/a | 401 | **SUSPECT** | Compares contact_status enum to lowercase non-member literals ('sent','followed_up',...) without ::text — Postgres enum error, route 500s. delivered hardcoded = sent. |
| `/api/v1/approvals` | GET,POST | token API key + read/wri | manual | 401 | **SUSPECT** | Agent column claim partially refuted: requested_at/expires_at/answered_at DO exist. jsonb_set-on-text claim unverified; keep SUSPECT pending Phase 5 route walk. |
| `/api/v1/auth` | POST | token — API key sha256 l | manual | 401 | **WIRED** | No rate limit on validation attempts; skips owner-domain join middleware does, so keys of demoted/off-domain owners still validate. |
| `/api/v1/campaigns` | GET,POST | token API key; POST writ | manual | 401 | **WIRED** | GET skips scope check (any valid key). POST contacts inserted without phone format validation; !dailyVolumeMax rejects 0. |
| `/api/v1/contacts` | GET,POST | token API key; POST writ | manual | 401 | **SUSPECT** | POST inserts gen_random_uuid()::text into leads.id (serial integer) — insert always fails. GET works. POST reuses write:campaigns scope instead of a contacts scope. |
| `/api/v1/conversations` | GET | token API key + read:con | n/a | 401 | **WIRED** | Org scoping via leads.organization_id (exists per m030); LEFT JOIN made effectively inner by WHERE. |
| `/api/v1/webhooks` | GET,POST | token API key, no scope  | manual | 401 | **WIRED** | No scope enforcement on read or write; url presence-checked only (SSRF-able target), events array unvalidated. |

**Pages:** all 43 pages + `/sitemap.xml` + `/robots.txt` returned **200** on the live sweep (dev-mode; authed pages render shell + client-side gate — server enforcement lives on the APIs).
## 3. Capabilities matrix (what Phases 1–4 must extend vs build — NO DUPLICATION)

Verdicts from the 7-capability audit, spot-verified against the live DB (59-table list, column checks). EXISTS = wired end-to-end; PARTIAL = surface exists, gap named; MISSING = build it; BROKEN = exists but cannot work as shipped.

### Contact (Phase 1)
| Item | Status | Evidence / gap |
|---|---|---|
| /contact page + real form → POST /api/contact → `contact_messages` | EXISTS | Full path wired; 201 + row insert |
| Validation | PARTIAL | Manual if-checks; comment claims zod, **no zod in repo** |
| Rate limiting | PARTIAL | In-memory Map keyed on client-controlled `x-forwarded-for` — resets per process, trivially bypassed; `rateLimitDb()` references a **`rate_limits` table that exists in no migration** |
| Operator notification | MISSING | Literal `// TODO` at contact/route.ts:63 — messages rot in the table |
| Honeypot/spam guard | MISSING | none |

### Reviews (Phase 2 — FTC 16 CFR 465)
| Item | Status | Evidence / gap |
|---|---|---|
| `reviews` table + moderation queue + audit rows | EXISTS | 033 (fixed #24/#25); /api/admin/reviews wired |
| Public GET /api/reviews | BROKEN | **500 in every env** — string filter bound as SQL parameter (neon 0.10.4 has no fragment API) + ambiguous unqualified columns (bug #27, observed) |
| Public /reviews page | BROKEN | Renders, but logic **inverted**: count>0 shows the empty state; no code path renders review cards |
| Purchase-verified submission | MISSING | POST consults no billing table; `verified_customer` never derived |
| One review per user | FIXED | #25 — converged unique index `uniq_reviews_user` |
| schema.org AggregateRating (real only) | MISSING | no structured data at all |
| SAMPLE DATA banner (non-prod) | MISSING | none |
| Demo seed guards | PARTIAL | ALLOW_DEMO_SEED + NODE_ENV guard real; **but seeds fabricated rows as `verified_customer=true, status='approved'`** — indistinguishable from genuine once #27 is fixed → must seed `is_demo=true, verified_customer=false` and the page must badge demo rows |
| CI guard on ALLOW_DEMO_SEED in prod env | MISSING | none |

### Legal walls (Phase 3)
| Item | Status | Evidence / gap |
|---|---|---|
| 9 legal pages routed + 200 | EXISTS | live sweep: all 200 |
| Versioned source of truth | BROKEN | Pages are **hardcoded TSX**; the `legal_documents` DB path (migration 032 + seed + /api/legal + LegalDocRenderer) is fully **orphaned** — seed never executed by any runner (live: `legal_documents` = 0 rows), no page reads it |
| Placeholders (`LEGAL_ENTITY_NAME` etc.) | MISSING | entity name hardcoded; no SUPPORT_EMAIL anywhere |
| Attorney-review frontmatter | PARTIAL | inconsistent |
| Signup ToS/Privacy acceptance rows | MISSING | live: `legal_acceptances` = 0 rows ever; the only writer is **unauthenticated POST /api/legal taking userId from the body** (forgeable — critical) |
| Re-accept interstitial on version bump | MISSING | no middleware version check |
| Messaging Compliance Agreement gate | MISSING | campaign launch has zero acceptance check |
| Footer links | PARTIAL | /legal/disclaimers unlinked; authed app has NO footer/legal links; footer hardcodes **"SOC 2", "10DLC Registered", "A2P Compliant"** — unsubstantiated public compliance claims, must be removed (pre-A2P!) |
| Cookie banner | PARTIAL | banner stores choice; **nothing ever reads it**; no script gating |
| Contradictory refund policy | BROKEN | DB seed says 7-day refund; /legal/refunds page says 30-day money-back |

### Admin + RBAC (Phase 4)
| Item | Status | Evidence / gap |
|---|---|---|
| requireAdmin on all 9 /api/admin/* | EXISTS | verified per-handler; DB role re-read each call |
| Roles | PARTIAL | only ADMIN/MEMBER; no owner/support tier |
| Admin UI | MISSING | **no /admin route group at all** — the admin APIs are curl-only |
| admin_audit_log writes | BROKEN | only writer is the finance export whose `transactions` query 500s first → viewer always empty |
| Bans | BROKEN | `bans` table **exists in no migration** (live 59-table list confirms) → all 3 handlers 500; no session revocation on ban; login hooks don't check bans |
| Kick / role demotion revocation | EXISTS | users/[id] PATCH deletes sessions; last-admin lockout guard |
| Force password reset / GDPR delete | MISSING | none |
| Refunds | BROKEN | /api/payments/refund = ledger status flip; **no Stripe/mock driver call, no requireAdmin, reason optional** |
| Finance/tax export | BROKEN | queries nonexistent `transactions` table → always 500 |
| Support read-only view | MISSING | none |

### Send path + webhooks (Phases 3/5 — compliance-critical)
| Item | Status | Evidence / gap |
|---|---|---|
| DNC + quiet hours server-side at transmit | EXISTS | dispatchGate in both send paths (prior live-proven work intact) |
| Inbound STOP → suppression (real Twilio path) | BROKEN | opt-out detection only wired into the simulator/JSON branch; **form-encoded (real Twilio) branch never writes suppression** AND its dedup query reads `audit_logs.metadata` — live column is `payload` → **500 on every real Twilio webhook** |
| Delivery status callbacks | MISSING | **no receiver exists** (no /api/sms/status; TwilioAdapter.validateWebhook has zero callers and is `return !!body.MessageSid` — not cryptographic) → message_events can never reach delivered/failed |
| Stripe webhook signature | BROKEN | default mock verifyWebhook returns true; live driver compares literal `'stripe-valid'` string → forgeable in every configuration |
| Stripe replay idempotency | PARTIAL | held-path returns 200 without recording event id |
| E-sign webhook | BROKEN | provider chosen by **client-supplied `x-esign-provider` header** defaulting to mock-accept-all; unauthenticated contract state machine; `collect_now` branch reads columns the SELECT never fetched (dead) |
| mock-checkout/complete + esign/mock-sign | BROKEN | **unauthenticated, no NODE_ENV gate, prod-reachable** (mock is the default provider) — anyone can flip payments to paid / drive contract signatures; reflected-XSS in both mock HTML pages |
| SMS mode isolation | PARTIAL | resolveSmsMode effectively dead; mode decisions scattered across jobs.ts/messaging.ts/dispatchGate.ts — violates Prompt 2's byte-identical-path rule; consolidation required before Prompt 2 |

## 4. Dead code / orphaned surfaces

| Item | Recommendation |
|---|---|
| `D:\anything\anything\` — 231-file stale partial repo self-copy (untracked, unreferenced) | DELETE (owner confirm) |
| `apps/web/scripts/apply-migrations-033-034.mjs` — bypass runner that masked #23/#24 | DELETE |
| `src/components/pages/` — 7 orphaned duplicate page components (already drifting) | DELETE |
| Campaign-wizard "Resurrection"/"A/B variants" toggles → columns persisted, **consuming engines were deleted** | REMOVE toggles (ghost UI) |
| Sidebar "System Health" links to `/health`; page lives at `/system-health` | FIX (1-line) |
| `/funnel` page + API work but have **zero inbound links** (v6 P4 unreachable) | ADD nav entry |
| `/compliance` marketing page unlinked & duplicates `/trust` | MERGE/redirect |
| `dropzone.tsx`, `PaymentChip.tsx` (unwired P2 surface), ~30 unimported ui primitives | DECIDE in phases |
| Stale .env.example entries TWILIO_TEST_*/TOLL_FREE_* (zero refs) | Phase 5 cleanup |
| Loose debug scripts at apps/web root touching credentials | MOVE/DELETE |

## 5. Corrections to agent findings (live evidence beats static reads)

- `/api/negotiation/sessions` — agent claimed "session start always 400 (missing columns)". **REFUTED**: Phase A was live-proven (a4-verify 9/9, observed offers); ranges live in `negotiation_price_ranges`.
- `/api/v1/approvals` — agent claimed `requested_at/expires_at/answered_at` don't exist. **REFUTED**: all three exist (live information_schema). Route stays SUSPECT only for its unverified jsonb_set usage.

## 6. Where the bugs went

#23–#28 recorded in BREAKAGE_TABLE.md with observed repros (4 FIXED+PROVEN, 2 OPEN). The Phase-0 static findings above are tracked as OPEN rows #29–#36 (grouped by subsystem) — each assigned to the phase that owns the fix. Unreproduced static claims stay SUSPECT in this doc and are NOT claimed as confirmed bugs.

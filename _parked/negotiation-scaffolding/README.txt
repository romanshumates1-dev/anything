Parked 2026-07-16: incomplete parallel-session negotiation scaffolding.
Broken (profiles/route.test.ts imports a non-existent route.ts), and uses
the AI-generates-numbers architecture that v3 Phase N supersedes with a
deterministic OWNER-ONLY suggestion engine (AI never emits a number).
Preserved for reference while building Phase N to the v3 spec. Not deleted.
